sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t poolep/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true poolep/docker-react npm run test
